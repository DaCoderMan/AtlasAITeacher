# Destination routing activation

Neon is the canonical structured store and is already active. External route delivery is enabled per destination by configuring the corresponding Atlas route webhook and credentials: Notion, Google Drive, GitHub, Google Calendar, and the compatible ChatGPT-memory bridge. Unconfigured destinations remain visible in routing/reconciliation rather than being silently treated as delivered.
